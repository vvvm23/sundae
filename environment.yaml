name: sundae
channels:
  - defaults
dependencies:
  - python=3.9
  - pytorch
  - cudatoolkit=11.3
  - tqdm
  - toml
  - ipython
  - einops
  - pip:
    - tokenizers
    - datasets
    - x-transformers
    - ptpt=0.0.28
    - wandb
prefix: /home/alex/.conda/envs/sundae
